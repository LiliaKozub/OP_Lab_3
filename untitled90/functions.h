#include "Point.h"

int number_of_point(); //функція для введення кількості точок
void input_points(class Point a[], int N); //функція для введення координат точок
int count_point(class Point a[], int N); //функція для підрахунку точок, які належать 1-ому октанту